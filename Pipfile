[[source]]
name = "pypi"
url = "https://${PYPI_USERNAME}:${PYPI_PASSWORD}@pypi.org/simple/"
verify_ssl = true

[packages]
jsonschema = "==4.4.0" 
mypy = "==0.930" 
openai = "==0.16.0" 
python-dotenv = "==0.19.2" 
tomli = "==1.2.3" # just for conflict resolution

[dev-packages]
black = "==21.12b0"
isort = "==5.10.1"
pylint = "==2.12.2"
pytest = "==6.2.5"
pytest-cov = "==3.0.0"
pytest-mock = "==3.6.1"
python-semantic-release = "==7.23.0"
types-jsonschema = "==4.3.2"

[scripts]
check-types = "python -m mypy conversation_assistant handler.py"
format = "bash -c 'python -m black conversation_assistant && python -m isort conversation_assistant'"
install = "pipenv install --dev"
lint = "python -m pylint ./conversation_assistant ./handler.py"
publish = "semantic-release --verbosity=DEBUG publish"
test = "python -m pytest conversation_assistant --cov=./conversation_assistant --cov-fail-under=90 --cov-branch --cov-report xml:cov.xml"

[requires]
python_version = "3.9"

[pipenv]
allow_prereleases = true
